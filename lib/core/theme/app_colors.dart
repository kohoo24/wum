import 'package:flutter/material.dart';

class AppColors {
  // Brand Colors
  static const primary = Color(0xFF0066FF);
  static const secondary = Color(0xFF5B8DEF);

  // Background Colors
  static const background = Color(0xFFF8FAFC);
  static const surface = Color(0xFFFFFFFF);
  static const surfaceVariant = Color(0xFFF3F4F6);

  // Text Colors
  static const textPrimary = Color(0xFF333333);
  static const textSecondary = Color(0xFF666666);
  static const textTertiary = Color(0xFF999999);

  // Category Colors
  static const restaurant = Color(0xFFFF6B6B);
  static const cafe = Color(0xFF4ECDC4);
  static const bar = Color(0xFF9B59B6);

  // Status Colors
  static const success = Color(0xFF4CAF50);
  static const error = Color(0xFFEF4444);
  static const warning = Color(0xFFFFB300);
  static const info = Color(0xFF2196F3);

  // UI Element Colors
  static const divider = Color(0xFFEEEEEE);
  static const cardBorder = Color(0xFFE2E8F0);
  static const shadow = Color(0x1A000000);

  // Map Marker Colors
  static const markerBackground = Color(0xFFFFFFFF);
  static const markerShadow = Color(0x29000000);

  // Rating Colors
  static const starFilled = Color(0xFFFBBF24);
  static const starEmpty = Color(0xFFE2E8F0);

  // Glass Effect Colors
  static const glassEffect = Color(0x0AFFFFFF);
  static const glassBorder = Color(0x1AFFFFFF);

  // Gradient Colors
  static const gradientPrimary = [
    Color(0xFF0066FF),
    Color(0xFF5B8DEF),
  ];

  static const gradientSurface = [
    Color(0xFFFFFFFF),
    Color(0xFFF8FAFC),
  ];
}
